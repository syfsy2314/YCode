# YCode 项目指引

## 项目概述

YCode 是一个使用 Python 实现的终端 AI 编程助手，产品形态类似 Claude Code。

## 沟通与代码风格

- 使用中文与用户沟通。
- Python 标识符使用英文。
- 新增代码注释使用中文，保持简洁，只解释不明显的设计意图。
- 遵循仓库已有的代码结构和风格；不要为未确认的未来需求提前设计。

## 学习笔记维护

- 每次用户要求整理并写入项目学习笔记时，在修改 `docs/notes/` 前必须重新读取当前工作区的最新状态。
- 至少检查当前项目文件结构、与本次主题直接相关的实现文件、最新功能文档和必要测试；不能只依赖此前会话内容、已有笔记或记忆中的旧实现。
- 以当前工作区中的实现代码为最终事实来源。功能文档与实现存在差异时，先确认最新已批准设计和实际代码，再据此整理笔记。
- 写入前检查已有笔记是否包含过时的类名、事件模型或调用链；发现不一致时，应同步修正相关内容并向用户说明。

## Spec 驱动开发

涉及下列任务时，在修改实现文件前必须阅读并遵循 `docs/Development-Workflow.md`：

- 新增或修改功能
- 修复缺陷
- 重构代码
- 修改会影响运行行为的配置
- 新增或修改产品行为测试

解释、审查、诊断、状态查询等只读任务不需要读取该文档，除非用户明确要求进入开发流程。纯拼写、排版和注释修正也不启动完整流程。

如果本次会话已经完整读取过该文档，并且文件没有变化，不要重复读取。

每个功能的开发文档存放在 `docs/features/<feature-name>/`，包括：

- `spec.md`：需求与验收标准
- `plan.md`：技术设计
- `task.md`：实现任务与验证方式
- `checklist.md`：最终验收项目

四份文档必须依次生成并获得用户明确批准。在全部批准之前，不得编写实现代码。

如果功能目录已经存在，先读取现有文档，从最后一个已批准阶段继续；不要重新生成或覆盖已经批准的内容。当前要求与已批准文档冲突时，指出具体冲突并等待用户决定。

## 实现原则

- 只实现已批准 Spec 中明确要求的内容。
- 按 `task.md` 的依赖顺序实施。
- 每完成一个任务，都运行其中定义的验证步骤；验证失败时先修复并重新运行。
- 除非用户明确要求，否则不要创建 Git commit。

## 验证与验收

- 使用项目现有的测试、格式化和静态检查命令；工具链确定后，在本文件补充准确命令。
- 格式检查：`.venv\Scripts\python.exe -m ruff format --check .`
- 静态检查：`.venv\Scripts\python.exe -m ruff check .`
- 完整测试：`.venv\Scripts\python.exe -m pytest -q`
- 编译检查：`.venv\Scripts\python.exe -m compileall -q ycode tests`
- 涉及 CLI、TUI、工具调用或对话流程的修改，必须执行真实的端到端交互测试。
- 环境支持时可以使用 tmux；不支持时使用等价的 PTY 或交互式终端会话。
- 按功能目录中的 `checklist.md` 逐项验证。
- 先运行验证并观察实际结果，再报告结论；最终报告列出执行的命令、实际结果和未通过项目。
